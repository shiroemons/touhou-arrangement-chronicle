# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Product {
  id: ID!
  name: String!
  shortName: String!
  productType: String!
  seriesNumber: Float!
}

type OriginalSong {
  id: ID!
  product: Product!
  name: String!
  composer: String!
  arranger: String!
  trackNumber: Int!
  duplicate: Boolean!
}

type Query {
  products: [Product!]!
  originalSongs: [OriginalSong!]!
}
